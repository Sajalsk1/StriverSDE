#include <bits/stdc++.h>
using namespace std;
int main()
{
    double x,ans=1.0;
    int p;
    
    cin>>x>>p;
    
    for (int i=0;i<p;i++)
    {
        ans=ans*x;
    }
    
    cout<<fixed<<setprecision(5)<<ans<<endl;
}